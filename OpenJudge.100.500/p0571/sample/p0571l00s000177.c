#include<stdio.h>
int main()
{
    int n,tr,tx,tt;
    scanf("%d %d %d",&n,&tr,&tx);
    tt=tr*n;
    if(tt>tx)
    {
        printf("%d",tx);
    }
    else if(tt<tx)
    {
        printf("%d",tt);
    }
    else
    {
        printf("%d",tt);
    }
    return 0;












}
