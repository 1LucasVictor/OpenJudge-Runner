#include <stdlib.h>
#include <stdio.h>
int main(){
  int n,a,b;
  if(scanf("%d %d %d", &n, &a, &b)){

  }else{

  }
  if((a * n) < b){
    printf("%d", a * n);
  }else{
    printf("%d", b);
  }
  return 0;
}
