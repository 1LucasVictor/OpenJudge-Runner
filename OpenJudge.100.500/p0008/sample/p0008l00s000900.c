#include <stdio.h>
const int MAX_V = 1000000; // 100????????§?´???°??¨?????????
int main() {
    int i, k,a,sum=0;
    int prime[MAX_V+1]; // 1????´???°???0????´???°??§?????? 
    int pcount[MAX_V+1];
    
    for(i = 2; i <= MAX_V; i++) {
        prime[i] = 1; // 2??\????´???°??¨??????
    } 
    for(i = 2; i*i <= MAX_V; i++) {
        if(prime[i]) {
            for(k = 2 * i; k <= MAX_V; k += i) {
                prime[k] = 0; // ?´???°2?????\????????°?´???°??§?????? 
            }
            
        } 
        
    }
    prime[0]=0;
    prime[1]=0;
    pcount[0]=0;
    pcount[1]=0;
    for(i=2;i<MAX_V;i++){
        if(prime[i]){
            sum++;
        }
        pcount[i]=sum;
    }
    while(scanf("%d",&a) != EOF){
        
        printf("%d\n",pcount[a]);
    } 

    return 0; 
    
}