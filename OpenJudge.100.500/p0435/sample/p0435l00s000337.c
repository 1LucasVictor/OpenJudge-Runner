#include<stdio.h>
#include<math.h>
int main()
{
	float n,d,x,y,c;
	int a=0;
	if(scanf("%f %f",&n,&d)){
	};
	while(n--)
	{
	if(scanf("%f %f",&x,&y)){
	};
	c=sqrt(x*x+y*y);
	if(c<=5)
	a++;
}printf("%d",a);
return 0;
 } 