#include <stdio.h>
#include <stdlib.h>
int main(void){
	int i,len;
	char str[1200];
	fgets(str,sizeof str,stdin);
	//sscanf(str,"%d %d",&a,&b);
	//scanf("%s",str);
	len=strlen(str);	//????????°??°???
	//printf("len=%d\n",len);
	for(i=0;i<len;i++){
		if( isupper(str[i]) ){	//??§???????????¨???
			str[i]=tolower(str[i]);//??§??????????°??????????
		}
		else{
			str[i]=toupper(str[i]);//?°????????????§?????????
		}
	}
	
	printf("%s",str);
	
	
	
	return 0;
}