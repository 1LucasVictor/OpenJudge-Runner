#include <stdio.h>

#define SIZE_OF_STRING	1200	//??\?????????????????????

int main(void)
{
	int loop;
	char string_array[SIZE_OF_STRING];

	fgets(string_array, SIZE_OF_STRING, stdin);

	//?°?????????¨??§???????????\????????????
	for(loop = 0; string_array[loop] != '\0'; loop++){
		
		if( ('A' <= string_array[loop]) && (string_array[loop] <= 'Z')){

			//??§???????????´??????????°????????????????
			string_array[loop] = string_array[loop] + ( 'a' - 'A' );

		}else if(('a' <= string_array[loop]) && (string_array[loop] <= 'z')){

			//?°????????????´???????????§???????????????
			string_array[loop] = string_array[loop] - ( 'a' - 'A' );

		}else{
			//do nothing
		}

	}

	printf("%s", string_array);

	return 0;
}