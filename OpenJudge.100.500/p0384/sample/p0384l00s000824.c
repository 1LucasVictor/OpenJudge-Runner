#include <stdio.h>
#include <ctype.h>

int main(void){

	char ch;

	do{
		scanf("%c", &ch);

		if(islower(ch)){
			ch = toupper(ch);	//?°???????????????§???????????????
		}
		else if(isupper(ch)){
			ch = tolower(ch);	//??§?????????????°????????????????
		}

		printf("%c" ,ch);

	}while(ch != '\n');

	return 0;
}