#include <stdio.h>
int main(void){
	int a,b,c,d,e,f;//??£???????¨????????????°???ax+by=c, dx+ey=f)
	double x,y;//??£???????¨????????§£

	while(scanf("%d%d%d%d%d%d",&a,&b,&c,&d,&e,&f) != EOF){
		fflush(0);
		x=(b*f-c*e)/(b*d-a*e);
		y=(c*d-a*f)/(b*d-a*e);
		printf("%.3lf %.3lf\n",x,y);
	}
	return 0;
}