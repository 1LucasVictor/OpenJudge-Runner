#include <stdio.h>
#include <string.h>
#include <math.h>

int main()
{
  int A, B, C;

  if (scanf("%d", &A) == 1)
    ;

  if (A >= 30)
  {
    printf("Yes");
  }
  else
  {
    printf("No");
  }

  return (0);
}
