
/*

??????????????贈????????????

?????????Run time error

C?即??????則???????????????????????????????????贈??\??????????????卒???????????????????????則'\0'?????\??????????????属???????????????
??\???????????即???????????卒????????????????????贈???????????????
strcpy()???strcat()???????????則????属????'\0'?????\??????????????????
C++??則???????????則?????贈???????????????


*/

#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main()
{
	char top[300];
	char bottom[300];
	char data[300];
	char none[1] = "";
	int kai,mai;
	int i,k,m,len;
	while(1){
		scanf("%s", data);
		if(data[0]=='-'){
			break;
		}
		len=strlen(data);
		scanf("%d", &kai);
		for(i=0; i<kai; i++){
			scanf("%d", &mai);
			for(k=0; k<mai; k++){
				top[k]=data[k];
			}
			top[mai] = '\0';
			m=0;
			for(k=mai; k<len; k++){
				bottom[m]=data[k];
				m++;
			}
			bottom[len-mai] = '\0';
			strcpy(data, bottom);
			strcat(data, top);
			top[0] = '\0';
			bottom[0] = '\0';
		}
		printf("%s\n", data);
	}
}