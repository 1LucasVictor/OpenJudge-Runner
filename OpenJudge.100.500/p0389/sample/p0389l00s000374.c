#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(void) {
    char tmp[1000];
    //???????????\???
    while(scanf("%s",&tmp)){
        //-????????????break
        if(tmp[0]=='-')break;
    	//???????????°??\???
        int loop,i,num,sum=0;
        scanf("%d",&loop);
        
        for (i=0;i<loop;i++){
            scanf("%d",&num);
            sum+=num;
        }
        //?????£???????????°
        sum=sum%strlen(tmp);
        //printf("%d",strlen(tmp));

        //?????£???????????????????????????
        for(i=sum;i<strlen(tmp);i++)  printf("%c",tmp[i]);
        for(i=0;i<sum;i++) printf("%c",tmp[i]);
        printf("\n");

    }
	return 0;
}