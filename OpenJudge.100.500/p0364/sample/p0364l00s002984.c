#include <stdio.h>
int main(void) {
	int W, H, x, y, r;

	scanf("%d %d %d %d %d\n", &W, &H, &x, &y, &r);

	if (-100<=x && x<=100 && -100<=y && y <= 100) {
		if (0 < W && W <= 100 && 0 < H && H <= 100 && 0 < r && r <= 100) {
			//?????????????????????????????????????????????????????¨???????????§??§?????????????????????
			//????????¢???????????¨?????§???????????????

			if (0 > (x - r) || W<(x + r) || 0>(y - r) || H < (y + r)) {
				printf("No\n");
			}

			else printf("Yes\n");

		}
		
	}

	return 0;
}