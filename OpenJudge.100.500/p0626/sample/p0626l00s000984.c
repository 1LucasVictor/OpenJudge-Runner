#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
#include <ctype.h>

int main(void) {
  int d, n, ans = 0;

  if(scanf("%d %d", &d, &n) == 1);

  ans = (int)pow(100, d) * n;

  printf("%d", ans);

  return 0;
}
