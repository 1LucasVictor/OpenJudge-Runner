#include <stdio.h>
int main(int argc, char const *argv[]) {

  int num_baggages; // ???????????°
  int num_trucks;   // ?????????????????°
  int p = 0;        // ?????????
  int i;
  int sum_w;        // ???????????????????????§????????????????????????????¨?
  int count_trucks;

  scanf("%d %d",&num_baggages,&num_trucks);
  int baggages[num_baggages]; // ??????????????????

  for( i = 0 ; i < num_baggages ; i++ ){
    scanf("%d",&baggages[i]);
  }

  int min = 1;
  int max = 1000000000;

  while( 1 ){

    // ?????????????????????1??°??????
    count_trucks = 1;
    sum_w = 0;
    p = (min+max)/2;

    for( i = 0 ; i < num_baggages ; i++ ){

      // ??????????????????????????????
      // ??????????????£????????\????????????????????????

      if( baggages[i] > p ){
        count_trucks = num_trucks + 1;
        break;
      }else if( sum_w + baggages[i] > p ){
        sum_w = baggages[i];
        count_trucks++;
      }else{
        sum_w += baggages[i];
      }

    }

    // ???????????????????????????????????°???????????????????????????????????°??¨?????£??????
    // ????????????????????????????¢???????????????????????????????????????´

    if( min == max ){
      p = min;
      break;
    }else if( count_trucks > num_trucks ){
      min = p+1;
    }else{
      max = p;
    }

    //printf("p2:%d\n",p);

  }

  printf("%d\n",p);

  return 0;
}