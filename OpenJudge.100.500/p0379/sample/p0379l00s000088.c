#include <stdio.h>
 
int main(void) {

	// ??????????????\??????????????°?????\???????????°
	int line;
	scanf("%d", &line);
	
	int column;
	scanf("%d", &column);
	
	int grid[line + 1][column];
	
/*	// ????¬???????????????????
	for (int left = 0; left <= line; left++) {

		for (int right = 0; right < column; right++) {

			grid[left][right] = 0;
		}
	}*/
	
	// ??\??????????????°?????????????????\??????
	for (int left = 0; left <= line; left++) {

		for (int right = 0; right < column; right++) {

			grid[left][right] = 0;
			scanf("%d", &grid[left][right]);
		}
	}

	// ????¨??????\?????????????????????
	int sum[line];
	
/*	for (int cnt = 0; cnt < line; cnt++) {
		
		sum[cnt] = 0;
	}
*/
	// ???????????????
	for (int cnt1 = 0; cnt1 < line; cnt1++) {
		
		sum[cnt1] = 0;

		for (int cnt2 = 0; cnt2 < column; cnt2++) {

			sum[cnt1] += grid[cnt1][cnt2] * grid[line][cnt2];

		}
		printf("%d\n", sum[cnt1]);
	}
	return 0;
}