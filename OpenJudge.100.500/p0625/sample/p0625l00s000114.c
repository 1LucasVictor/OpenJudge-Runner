#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    if(b<=8 && a<=8)

    printf("Yay!\n");
  else
    printf(":(");
        //printf("In this case, there is no way for them to take desired number of pieces, unfortunately.");

}
