#include<stdio.h>

int main (void){

	int a,b;
  
    scanf("%d %d",&a,&b);
    if(0<=a && a<=8 && 0<=8 && b<=8){
      printf("Yay!");
    }
    else{
      printf(":(");
    }
      
  
	return 0;
}
