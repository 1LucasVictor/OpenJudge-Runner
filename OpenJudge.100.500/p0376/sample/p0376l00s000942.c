#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(void) {
	char tmp[1000];
	//??\????????°?????\????????????
	fgets(tmp, sizeof(tmp), stdin);
	int loop = atoi(tmp);
	
	//??????????????????????????\???
	fgets(tmp, sizeof(tmp), stdin);
	int num[1000];
	
	//??\???????????????????????????
	num[0]=atoi(strtok(tmp," "));
	
	int i;
    for(i = 1;i<loop;i++){
        num[i]=atoi(strtok(NULL," "));
    }
	
	for (i = loop-1; i >=0;i--) {
	    if(i == 0) {
	        printf("%d\n",num[i]);
            break;
        }
		printf("%d ",num[i]);
	}
	//???????????????
	

	return 0;
}