#include <stdio.h>
#include <stdlib.h>
	/*??????????????°??????????????????atoi???????????????*/
#include <string.h>
	/*??????????????????????????????????????????strtok???????????????*/

int main(void)
{
	int n, i, num_i;
	char sn[7];

	fgets(sn, sizeof(sn), stdin);
	n = atoi(sn);
		/*scanf?????¨\n?????\?????????????????????????????§???fgets?????????*/

	int sl = 9 * 100;
	char i_str[sl], *snum;

	fgets(i_str, sizeof(i_str), stdin);

	snum = strtok(i_str, " \n");

	int r_nums[n];
	i = n - 1;

	while (snum != NULL) {
		num_i = atoi(snum);
		r_nums[i] = num_i;
		snum = strtok(NULL, " \n");
		i--;
	}

	for (i = 0; i < n - 1; i++)
		printf("%d ", r_nums[i]);

	printf("%d\n", r_nums[n - 1]);

	return 0;
}