#include <stdio.h>
#include <string.h>
#include <limits.h>
 
int array[5000];

int main(void)
{
  int n, i, s, e, sum, max;
 
  while(1){
    /* ??????????????????????????? */
    memset(array, 0, sizeof(array));
 
    /* ??\??????????????????????????? */
    scanf("%d", &n);
    if(n == 0) break;   /* n???0?????£???????????? */

    /* n????????´??°??????????????? */
    for(i=0; i<n; i++){
      scanf("%d", &array[i]);
    }
 
    /* ?????§??????????????????????????°???????????? */
    max = INT_MIN;
 
    /* ??¨???????????¨??¢?´¢ */
    for(s=0; s<n; s++){
      for(e=s; e<n; e++){
        sum = 0;
        for(i=s; i<=e; i++){
          sum += array[i];
        }
        if(max < sum) max = sum;
      }
    }
 
    /* ???????????¨??? */
    printf("%d\n", max);
  }
 
  return 0;
}