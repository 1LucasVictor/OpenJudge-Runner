#include<stdio.h>

/*x,y?????§?°?????????\???????????\??????????????????????????????????????????????????¨???????????§??¬?´???°???????????¢??°*/
int gcd(int x, int y) {
	int temp;
	while (x) {
		if (y > x) {
			temp = x;
			x = y;
			y = temp;
		}
		else if (x > y) {
			x = x%y;
		}
		
	}
		return y;
	
}

int main(void) {
	int x, y;
	scanf("%d %d", &x, &y);
	printf("%d\n", gcd(x,y));
	return 0;
}