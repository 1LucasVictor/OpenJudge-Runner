#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

int main()
{
       int x, a;

       scanf("%d %d", &x, &a );

       if( x < a ) puts("0");
       else puts("10");



       return 0;
}