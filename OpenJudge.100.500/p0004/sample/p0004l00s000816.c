#include <stdio.h>

int main(void)
{
	int a, b;
	int GCD, LCM;
	int j = 0;
	int i;
	int tmp;
	int r;
	
	while (scanf("%d %d", &a, &b) != EOF){
		
		for (GCD = 1; j == 0; GCD++){
			if (GCD % a == 0 && GCD % b == 0){
				j++;
			}
		}
		
		j = 0;
		
		  /* ?????¶??° a > b ????¢?????????\??? */
  if(a<b){
    tmp = a;
    a = b;
    b = tmp;
  }
 
  /* ?????????????????????????????? */
  r = a % b;
  while(r!=0){
    a = b;
    b = r;
    r = a % b;
  }

		j = 0;
		
		printf("%d %d\n", b, GCD - 1);
		
	}
	
	return (0);
}